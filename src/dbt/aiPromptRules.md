Infer each column's semantic category from its name, description, data types, and sample values; never infer it from row position.

Proposed names must be uppercase. Identifier columns use `ID_`; description/text columns use `DES_`; type/category columns use `TYP_`; no other prefixes are allowed.
