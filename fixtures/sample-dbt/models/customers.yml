version: 2
models:
  - name: customers
    description: One row per customer
    columns:
      - name: customer_id
        data_type: integer
        description: Primary key
        tests:
          - not_null
          - unique
      - name: email
        data_type: string
        description: Customer email address
